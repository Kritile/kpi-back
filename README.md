#### For generate ide helpers run:

```   
    php artisan ide-helper:generate
    php artisan ide-helper:model 
    php artisan ide-helper:meta 
```

#### For generate swagger docs run:
```
    php artisan l5-swagger:generate 
```

